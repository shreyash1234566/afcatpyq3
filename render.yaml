services:
  - type: web
    name: afcat-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python api_server.py
    envVars:
      - key: GROQ_API_KEY
        scope: build,runtime
